generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:itsnotthatserious@localhost:5432/postgres"
}

model User {
  id       String @id @default(uuid())
  username String
  password String
}
